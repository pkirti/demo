import React from 'react';
import { Typography } from '@material-ui/core';

const Home = () => {
  return (
    <div>
      <Typography variant="h4">Welcome to Retail Inventory Management</Typography>
      <Typography variant="body1">Manage your inventory efficiently and effectively.</Typography>
    </div>
  );
};

export default Home;
